<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="davekotowicz.as1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ListPurchases"
            android:label="@string/title_activity_list_purchases">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PurchaseEntry"
            android:label="@string/title_activity_purchase_entry">
        </activity>
        <activity
            android:name=".PurchaseEdit"
            android:label="@string/title_activity_purchase_edit">
        </activity>
    </application>

</manifest>
